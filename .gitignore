# Secrets
.env
backend/.env
*.local

# Runtime state + account data (real fills/deposits land here — never commit)
backend/data/

# Backups
*.bak

# Go
backend/server
backend/server.exe
backend/*.exe

# Node / frontend
node_modules/
frontend/dist/
frontend/node_modules/

# Editor / OS
.DS_Store
.vscode/
.idea/

# Large SIP data exports
Data-Sip/candles/
Data-Sip/logs/
Data-Sip/manifest/

# Python ML plane
ml/.venv/

# ML runtime artifacts (live signal outputs, bar logs, trained model binaries —
# regenerated by the trainers; trading-activity data never belongs in the repo)
ml/signal.json
ml/recent_bars.json
ml/*.bin

# Build artifacts
*.tsbuildinfo
ml/__pycache__/

# Local-only notes (2026-08-01). These stay on the machine but out of the repo:
# personal playbooks, the internal idea queue, a duplicate of CLAUDE.md, and the
# design docs for the AI quant system that was removed 2026-07-31. The retirement
# write-ups that DO belong in the repo are DIP_RISE_ARCHIVE.md, SNDK_RETIREMENT.md
# and AI_QUANT_LOG_DIGEST.md.
AGENTS.md
Instruction.md
RESEARCH_BACKLOG.md
QUANT_EXPLAINED.md
QUANT_VISION.md
STRATEGY_ENGINE.md
THROUGHPUT_MODE.md
